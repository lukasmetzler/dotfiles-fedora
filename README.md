## Usage

Clone this repository and run `chmod +x main.sh && ./main.sh` to automatically install applications, development tools, and configure Zsh with Oh My Zsh and Powerlevel10k theme. After the script completes, log out and back in, then run `p10k configure` to customize your terminal prompt.
